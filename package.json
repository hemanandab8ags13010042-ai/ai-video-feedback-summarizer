{
  "name": "ai-video-feedback-summarizer",
  "version": "1.0.0",
  "description": "Enterprise-grade SaaS app to analyze and summarize video feedback into editing and VFX tasks",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "db:migrate-to-cloud": "npm run db:migrate-to-cloud --prefix backend",
    "test:email": "npm run test:email --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
